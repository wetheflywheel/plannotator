# Session Handoff

**Date:** 2026-03-30 07:10 Asia/Ho_Chi_Minh
**Branch:** main
**Last commit:** 4f47fa8 feat: add git branch/commit/custom-build metadata to UI header

## What was done
- Pulled upstream v0.15.2 -> v0.15.5 (11 commits: Copilot integration, checkboxes, print, diff options, parser fixes, search perf)
- Rebuilt review app + hook + compiled binary
- Investigated "Error: network error" in plan review UI
- Discovered Claude Code hook uses `~/.ipe/ipe.bin` (NOT `~/.local/bin/plannotator`) — the old binary had custom code (Multi-LLM, Review only) not in this repo
- Replaced `~/.ipe/ipe.bin` with freshly compiled v0.15.5 binary
- Added build metadata to Vite configs and both editor headers: git branch, commit hash, "Custom Build" badge

## Decisions made
- The "Error: network error" and "Multi-LLM"/"Review only" buttons came from an older custom binary at `~/.ipe/ipe.bin` — code that has never existed in this repo's git history
- Build metadata shows as `v0.15.5 (main@9b69d17)` with amber "Custom Build" badge when HEAD isn't an exact release tag

## Unfinished threads
- The commit is local only (not pushed) — this is the upstream repo, user should decide whether to push or maintain as a local fork
- The old `~/.ipe/ipe.bin.bak` and `ipe.bin.old` files still exist (previous custom builds)

## Next steps
- Test plan review to confirm no "Error: network error" with new binary
- Decide whether to push build metadata commit upstream or keep local
- If the old Multi-LLM/Review-only features are wanted, their source needs to be found (not in this repo)
